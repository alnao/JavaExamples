<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
      fx:controller="it.alnao.javafx.weatherapp.controller.WeatherController"
      alignment="CENTER" spacing="20" styleClass="root">
    
    <!-- Titolo -->
    <Label text="🌤️ Weather App" styleClass="title"/>
    
    <!-- Card contenitore -->
    <VBox styleClass="card" maxWidth="500">
        <!-- Area di ricerca -->
        <HBox alignment="CENTER" spacing="10">
            <TextField fx:id="cityField" promptText="Inserisci città..." 
                      HBox.hgrow="ALWAYS" styleClass="input-field"/>
            <Button fx:id="searchButton" text="🔍 Cerca" 
                   onAction="#handleSearch" styleClass="search-button"/>
        </HBox>
        
        <!-- Loading indicator -->
        <ProgressIndicator fx:id="loadingIndicator" maxWidth="50" maxHeight="50"/>
        
        <!-- Error label -->
        <Label fx:id="errorLabel" styleClass="error-label" wrapText="true"/>
        
        <!-- Separatore -->
        <Separator/>
        
        <!-- Dati meteo -->
        <VBox spacing="15" alignment="CENTER" styleClass="weather-data">
            <Label fx:id="cityLabel" text="---" styleClass="city-label"/>
            <Label fx:id="temperatureLabel" text="--°C" styleClass="temperature-label"/>
            <Label fx:id="conditionLabel" text="---" styleClass="condition-label"/>
            
            <!-- Info aggiuntive -->
            <HBox spacing="30" alignment="CENTER">
                <Label fx:id="humidityLabel" text="Umidità: --%"/>
                <Label fx:id="windLabel" text="Vento: -- km/h"/>
            </HBox>
        </VBox>
    </VBox>
    
    <!-- Footer -->
    <Label text="Progetto JavaFX 10 - Full MVC Architecture" styleClass="footer"/>
</VBox>
